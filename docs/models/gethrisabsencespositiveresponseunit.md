# GetHrisAbsencesPositiveResponseUnit

The unit of time for this absence. Can be `HOURS` or `DAYS`.

## Example Usage

```python
from kombo.models import GetHrisAbsencesPositiveResponseUnit
value: GetHrisAbsencesPositiveResponseUnit = "HOURS"
```


## Values

- `"HOURS"`
- `"DAYS"`
