# GetAtsUsersPositiveResponseScope

Whether the role applies globally or is scoped to a specific job.

## Example Usage

```python
from kombo.models import GetAtsUsersPositiveResponseScope
value: GetAtsUsersPositiveResponseScope = "SYSTEM"
```


## Values

- `"SYSTEM"`
- `"JOB"`
